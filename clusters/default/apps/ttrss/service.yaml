apiVersion: v1
kind: Service
metadata:
  name: ttrss
  namespace: applications
spec:
  selector:
    app: ttrss
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9000
    NodePort: 31582
  type: NodePort

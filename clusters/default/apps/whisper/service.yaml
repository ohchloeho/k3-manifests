apiVersion: v1
kind: Service
metadata:
  name: whisper-service
  namespace: applications
  labels:
    app: whisper
spec:
  selector:
    app: whisper
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9000
    NodePort: 32003
  type: NodePort
